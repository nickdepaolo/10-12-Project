import React from "react";

const Failure = () => {
 
    
    return(
        <div>
            <p>Search crashes if it fails</p>
            <p>SetState for adv search could be better</p>
            <p>More needs set up in location search</p>
            <p>More needs set up in episode search</p>
        </div>
    )
}

export default Failure